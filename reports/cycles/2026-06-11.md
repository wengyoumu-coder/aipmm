# AI Growth Cycles — 2026-06-11

## Cycle 0003

- Human timestamp metadata: `2026-06-11 01:05` UTC+8
- Production: https://ai-web-observatory.aipmm.workers.dev
- Starting data: 50 real requests, 0 claimed AI identities, 0 verified AI signals,
  0 tool calls, 0 referrals, 0 measured crawl depth

### Hypothesis

Current crawlers can discover high-level machine resources such as
`/openapi.json`, but the site still lacks a compact example-driven tool
directory. Publishing one should make existing tools easier to continue into
for search systems and user-directed agents.

### Actions

- Reproduced a production measurement failure: `npm run report:raw` broke under
  local `Node v25.5.0` because `wrangler d1 execute --remote` returned
  `fetch failed`.
- Added a bounded runtime fallback so the raw exporter retries remote D1
  queries through `Node 22 + Wrangler 4.99.0` when the host Node version is
  newer than 22.
- Published `/tools` and `/api/v1/tools.json` as new machine-discovery
  resources with deterministic example requests and outputs for all three
  existing tools.
- Linked the new tool directory from the homepage, global navigation,
  footer machine resources, `llms.txt`, manifest, sitemap, static export, and
  agent card documentation.
- Extended smoke coverage to include the new HTML and JSON tool discovery
  resources.
- Submitted the changed canonical URL set through IndexNow.

### Verification

- Red-green TDD passed for the new exporter fallback behavior.
- 59 automated tests passed.
- Strict TypeScript checking passed.
- Static export generated 48 files.
- Production Worker deployment succeeded.
- Production smoke verification passed for 22 resources and tools.
- Production Worker version:
  `6c5043ca-d73a-4f47-b0f7-f44380a2f745`.
- IndexNow accepted the 40-URL submission with HTTP 200.
- `npm run report:raw` completed successfully after the fallback fix and wrote
  `reports/raw/2026-06-10-7d.json`.

### Visible Change

- The measurement loop is operational again in the current local environment,
  so future cycles can continue using `npm run report:raw` instead of stalling
  on the Node/Wrangler incompatibility.
- The public site now exposes a dedicated tool directory in both HTML and JSON,
  which creates a clearer continuation path from `openapi.json` to callable
  endpoints.
- The post-action raw snapshot still shows 50 real requests, 0 claimed AI
  identities, 0 verified AI signals, 0 tool calls, and 0 referrals. No
  traffic lift is claimed yet.

### Failures

- Remote D1 execution remains fragile under the host `Node 25` runtime, so the
  exporter currently relies on the explicit `Node 22` fallback for production
  reads.
- The new tool discovery resources had no observable crawler fetches in the
  immediate post-deploy snapshot.

### Next Observation

Watch for first fetches of `/tools` or `/api/v1/tools.json`, then check whether
any follow-on requests reach `/api/v1/tools/*`. If tool calls remain zero, the
next cycle should tighten per-tool continuation links or publish another
machine-usable resource that naturally chains into those endpoints.

## Cycle 0004

- Human timestamp metadata: `2026-06-11 06:00` UTC+8
- Production: https://ai-web-observatory.aipmm.workers.dev
- Mission: earn legitimate independent machine discovery, understanding,
  action, citation, and return by publishing useful capabilities and preserving
  honest evidence.

### Run-Before Evidence

- The immutable starting snapshot contained 67 real requests.
- **Verified signals:** production D1 recorded requests across public machine
  and document resources; no AI identity was independently verified.
- **Claimed User-Agent:** one US request at `2026-06-10T18:23:12.158Z`
  claimed `OAI-SearchBot` and fetched only `/robots.txt`. It had crawl depth 1
  and no later request from the same coarse identity.
- **Anonymous real behavior:** the other 66 requests reached the homepage,
  registry pages, recipe pages, `llms.txt`, and `openapi.json`, among other
  resources. Their operator or intent is unknown.
- **Internal or excluded behavior:** smoke requests and administrative reads
  were excluded. Two early curl checks accidentally recorded during this cycle
  were identified by exact timestamp, path, automation category, and shared
  coarse identity, then removed before the final snapshot.
- **Measurement gaps:** the database does not independently verify the network
  origin of claimed identities, and there was no evidence of tool selection,
  valid tool use, citation, or later return.

### Bottleneck Diagnosis

Observed behavior reaches **Retrieve**, and existing resources can support
**Understand**, but it stops before **Act**. The strongest current constraint is
the missing conversion from descriptive machine resources into an explicit,
auditable end-to-end task workflow.

### Candidate Actions

Scores are 1 to 5. For cost, 5 means lowest cost.

| Type | Candidate | Mission impact | Evidence fit | Learning value | Reversibility | Cost |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| Exploit | Add stronger continuation links from robots and sitemap resources into existing tools | 4 | 5 | 3 | 5 | 5 |
| Explore | Publish a compact `/skill.md` that chains registry, recipe selection, generation, linting, and cited output | 5 | 5 | 5 | 5 | 4 |
| Measure | Add independent network-origin verification for claimed crawler identities | 4 | 5 | 5 | 3 | 2 |

The explore option was selected because it creates a real user-directed agent
capability now, directly tests the `Understand -> Act` gap, and remains small
and reversible. Network verification remains important but available
Cloudflare signals require separate investigation and would not itself create
a useful action path.

### Hypothesis

A short skill with explicit applicability, inputs, ordered API calls, output
requirements, source usage, and identity-integrity constraints can help a
human-directed agent continue from discovery into a valid robots policy tool
workflow.

Expected future evidence is a real `/skill.md` retrieval followed by a valid
`POST /api/v1/tools/*` request. A skill fetch with continued zero tool calls
would weaken the workflow-conversion hypothesis.

### Actions

- Published `/skill.md` with YAML metadata, task inputs, an ordered workflow,
  deterministic generation and lint examples, completion requirements, and an
  explicit warning that a claimed User-Agent is not independently verified.
- Linked the skill from the homepage, tool directory, HTTP discovery headers,
  `llms.txt`, manifest, sitemap, agent card, changelog, and static export.
- Added route, manifest, agent-card, sitemap, static-export, and production
  smoke coverage.
- Submitted the changed 41-URL canonical set through IndexNow.

### Verification

- Red-green TDD reproduced the missing route, manifest entry, and static file,
  then passed after implementation.
- 60 automated tests passed.
- Strict TypeScript checking passed.
- Static export generated 49 files.
- Worker dry-run passed.
- The default Node 25 deployment failed at the Cloudflare API request with
  `fetch failed`; the known Node 22 + Wrangler 4.99.0 runtime path deployed
  successfully.
- Production smoke verification passed for 23 resources and tools.
- Production `/skill.md` returned HTTP 200 with
  `text/markdown; charset=utf-8` and the expected workflow.
- Production Worker version:
  `567ea6db-3ef6-4e9c-a918-b6fd813e6e6f`.
- IndexNow accepted 41 URLs with HTTP 200.

### System Change

The site now exposes one compact, executable workflow that connects sourced
identity data to recipe selection, tool calls, validation, and cited output.
This is a capability change, not evidence that an independent agent used it.

### Behavior Change

The final snapshot contained 68 real requests, including one new unknown
homepage request. It still contained only the single unverified
`OAI-SearchBot` claim, 0 tool interactions, 0 citation referrals, and 0
cross-cycle AI repeats. No organic `/skill.md` retrieval was observed, so no
behavior lift is attributed to the deployment.

### Failures and Ambiguity

- Live Wrangler operations remain unreliable under host Node 25 while the same
  operations succeed under Node 22.
- Claimed crawler identity still lacks independent origin verification.
- The immediate post-deploy window is too sparse to distinguish lack of
  discovery from lack of workflow value.

### Next Observation

Does an independent requester fetch `/skill.md`, and if so, does the same
coarse identity continue to a valid tool endpoint or a cited registry resource?

## Cycle 0005

- Human timestamp metadata: `2026-06-11 11:02` UTC+8
- Production: https://ai-web-observatory.aipmm.workers.dev
- Mission: earn legitimate independent machine discovery, understanding,
  action, citation, and return while preserving the distinction between real
  behavior and internal verification.

### Run-Before Evidence

- The immutable starting snapshot contained 84 real requests, up from 68 at
  the end of Cycle 0004.
- **Verified signals:** production D1 contained successful retrievals across
  public pages and machine resources. No AI identity was independently
  verified.
- **Claimed User-Agent:** the same single US request claimed
  `OAI-SearchBot`, fetched only `/robots.txt`, and had no cross-day repeat.
- **Anonymous real behavior:** 83 other requests included homepage,
  `robots.txt`, registry, recipe, `llms.txt`, `llms-full.txt`, and OpenAPI
  retrievals. New traffic included anonymous RU browser-classified machine
  resource reads and US homepage or robots reads, but no organic `/skill.md`,
  `/tools`, or tool endpoint request.
- **Internal or excluded behavior:** two direct checks made after the starting
  snapshot used `AI-Web-Observatory-Internal/1.0`, which was not yet excluded.
  The exact two rows were deleted, and the recorder was changed so subsequent
  smoke and direct checks do not enter D1.
- **Measurement gaps:** claimed identities still lack network-origin
  verification, raw reporting does not summarize anonymous request sequences,
  and anonymous browser or unknown classifications do not reveal operator
  intent.

### Bottleneck Diagnosis

Retrieval has reached machine resources that can support understanding, but
behavior still stops before **Act**. The strongest actionable constraint is
that the only complete workflow required a POST request, while independent
search and retrieval systems commonly limit themselves to safe GET requests.

### Candidate Actions

Scores are 1 to 5. For cost, 5 means lowest cost.

| Type | Candidate | Mission impact | Evidence fit | Learning value | Reversibility | Cost |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| Exploit | Add a deterministic GET invocation to the existing robots policy generator and publish an exact call URL | 5 | 5 | 5 | 5 | 4 |
| Explore | Publish another agent protocol or well-known tool description | 4 | 3 | 4 | 5 | 3 |
| Measure | Add anonymous journey summaries and stronger claimed-origin diagnostics to raw reports | 3 | 5 | 5 | 5 | 3 |

The exploit option was selected because it removes a concrete action barrier
from an already useful workflow without adding another descriptive surface.
The measure option remains valuable, but it would not itself make the tool
callable by retrieval-only clients.

### Hypothesis

If the deterministic policy generator is callable with a cacheable GET URL and
that exact URL is exposed in the tool catalog, OpenAPI, HTML directory, and
skill, an independent retrieval system can complete a real tool action without
needing permission to send a POST body.

Expected future evidence is an organic GET request to
`/api/v1/tools/generate-robots` followed by a recipe, registry, or later tool
request. Continued machine-resource retrieval with zero tool requests would
weaken the action-friction hypothesis. A GET tool request with no continuation
would move the suspected constraint toward workflow value or return utility.

Safety constraints were unchanged: no manufactured requests, spoofed
identities, third-party posting, or internal verification counted as natural
behavior.

### Actions

- Added `GET /api/v1/tools/generate-robots?preset=...&sitemap=...` using the
  same validation and response logic as the existing POST tool.
- Published the exact retrieval-safe URL in `/tools`, `/api/v1/tools.json`,
  `/skill.md`, and the OpenAPI GET contract.
- Added the GET invocation to production smoke coverage and changelog
  resources.
- Excluded the explicit `AI-Web-Observatory-Internal/1.0` verification
  identity from analytics alongside the existing smoke identity.
- Submitted 41 current canonical URLs through IndexNow so changed discovery
  resources can be revisited.

### Verification

- Red-green TDD reproduced the GET 404, missing discovery metadata, and
  internal-check analytics leak before implementation.
- 61 automated tests passed.
- Strict TypeScript checking passed.
- Static export generated 49 files.
- Worker dry-run passed.
- Production smoke verification passed for 24 resources and tools.
- Production GET generation returned the expected recipe, sitemap, lint
  summary, and continuation links.
- Production tool JSON exposed `methods: ["GET", "POST"]` and the exact
  `readUrl`; OpenAPI exposed both GET and POST.
- Production D1 showed no tool rows from smoke or direct verification.
- Production Worker version:
  `e45e6d85-fd71-4070-b2cd-2392b68322af`.
- IndexNow accepted 41 URLs with HTTP 200.

### System Change

The existing end-to-end policy workflow can now be executed by clients that
only retrieve safe GET resources. Internal direct checks are also excluded
deterministically instead of requiring later D1 cleanup.

### Behavior Change

The final raw snapshot contained 85 real requests, one more than the starting
snapshot. The new request was an anonymous US homepage GET and cannot be
attributed to the deployment. The snapshot still contained one unverified
`OAI-SearchBot` claim, 0 organic tool interactions, 0 citation referrals, and
0 cross-cycle AI repeats.

### Failures and Ambiguity

- The first two direct checks used an internal User-Agent that the old
  production recorder did not exclude; both rows were removed exactly and the
  exclusion was then tested and deployed.
- Live Cloudflare operations still require the Node 22 + Wrangler 4.99.0 path
  because host Node 25 fetches remain unreliable.
- There is still no evidence that an independent requester discovered the
  skill or retrieval-safe tool call.

### Next Observation

Does an independent requester use the published GET generator URL, and if so,
does the same coarse identity continue to a recipe, registry source, later tool
call, citation, or cross-cycle return?

## Cycle 0006

- Human timestamp metadata: `2026-06-11 16:03` UTC+8
- Production: https://ai-web-observatory.aipmm.workers.dev
- Mission: help independent AI and search systems move from legitimate
  discovery and understanding into useful tool action, return, or citation
  without manufacturing evidence.

### Run-Before Evidence

- The immutable starting snapshot contained 90 real requests.
- **Verified signals:** production D1 contained successful public retrievals;
  no AI identity was independently verified.
- **Claimed User-Agent:** one US request still claimed `OAI-SearchBot`, fetched
  only `/robots.txt`, and had no cross-day repeat.
- **Anonymous real behavior:** 89 other requests included multi-resource
  journeys. One coarse RU browser identity had reached recipe pages, `/tools`,
  and finally `/skill.md` without calling a tool. Another RU identity retrieved
  recipe JSON, `openapi.json`, registry JSON, `llms-full.txt`, and the manifest
  without calling a tool.
- **Internal or excluded behavior:** production checks used
  `AI-Web-Observatory-Internal/1.0` or the excluded smoke identity. A post-check
  D1 query found no verification rows and no tool rows from those checks.
- **Measurement gaps:** claimed identities still lack independent origin
  verification, and the raw report does not summarize anonymous journeys even
  though targeted D1 queries can reconstruct them.

### Bottleneck Diagnosis

Behavior now demonstrably reaches machine resources that describe the workflow,
including `/tools` and `/skill.md`, but still stops before **Act**. The most
specific actionable constraint was that recipe resources already receiving
traffic still exposed either a generic tool endpoint or a POST instruction,
not a recipe-specific retrieval-safe action URL.

### Candidate Actions

Scores are 1 to 5. For cost, 5 means lowest cost.

| Type | Candidate | Mission impact | Evidence fit | Learning value | Reversibility | Cost |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| Exploit | Link every recipe representation directly to its preset-specific GET generator call | 5 | 5 | 4 | 5 | 5 |
| Explore | Publish another agent protocol or machine workflow format | 4 | 2 | 4 | 5 | 3 |
| Measure | Add anonymous journey summaries to the raw report | 3 | 5 | 5 | 5 | 4 |

The exploit option was selected because observed anonymous requesters already
reached recipes, the tool directory, and the skill. It removes the smallest
remaining construction step from those proven retrieval paths. Journey
reporting remains useful, but it would not itself create an action.

### Hypothesis

If each recipe's HTML, JSON, and Markdown representation contains the exact
safe GET URL for that preset, a retrieval-only requester can execute the
selected policy without constructing a POST body or query string.

Expected future evidence is an organic GET request to
`/api/v1/tools/generate-robots?preset=...`, ideally followed by a source,
another tool, citation, or later return. Continued recipe, tool, or skill
retrieval with zero tool interactions would weaken the last-mile discoverability
hypothesis and shift suspicion toward practical workflow value.

Safety constraints remained unchanged: no synthetic organic requests, spoofed
identities, third-party promotion, or internal checks counted as behavior.

### Actions

- Added one shared recipe-specific generator path builder.
- Added clickable preset-specific GET links to the recipe HTML index and detail
  pages.
- Added absolute `generateUrl` fields to recipe collection and detail JSON.
- Added exact GET generator URLs to every recipe in `/robots-recipes.md`.
- Published the change in the RSS and HTML changelog.
- Submitted the current 41 canonical URLs through IndexNow.

### Verification

- Red-green TDD first reproduced missing recipe-specific links in collection,
  detail, JSON, Markdown, and changelog responses.
- 61 automated tests passed.
- Strict TypeScript checking passed.
- Static export generated 49 files.
- Worker dry-run passed.
- Production deployment succeeded with Worker version
  `7fcb8b07-7e08-4c80-8bff-c102d2bdcb3b`.
- Direct production checks confirmed the new HTML, JSON, Markdown, and GET tool
  responses.
- The default smoke command failed because Node 25 `fetch` inherited local
  proxy variables. Re-running with `all_proxy`, `http_proxy`, and `https_proxy`
  unset passed all 24 production checks. A direct Node 25 Wrangler D1 query
  also succeeded with those variables unset, identifying the runtime issue as
  proxy-environment interaction rather than a production outage.
- IndexNow accepted 41 URLs with HTTP 200.
- D1 contained no internal verification events after the checks.

### System Change

Every published recipe representation now carries a directly executable,
recipe-specific safe GET action. A machine consumer that has already selected a
recipe no longer needs to infer the preset parameter or switch to POST.

### Behavior Change

The final snapshot contained 94 real requests, 1 unverified claimed AI request,
0 organic tool interactions, 0 citation referrals, and 0 cross-cycle AI
repeats. Four anonymous requests arrived during execution, but none called a
tool and none can be attributed to the deployment. The system changed; no
behavior lift is claimed.

### Failures and Ambiguity

- Recipe, tool, and skill retrieval has now occurred without action, so
  capability discovery alone is a weaker explanation for zero tool use.
- Anonymous journeys are visible only through ad hoc D1 queries, not the
  standard raw report.
- Proxy variables can make Node 25 live checks look like platform failures;
  future diagnostics must test the same command with proxy variables unset
  before falling back to another runtime.

### Next Observation

Does any independent requester follow a new recipe-specific
`generate-robots?preset=...` URL, and if recipe/tool/skill retrieval continues
without action, what evidence would distinguish low workflow value from a
remaining invocation-format barrier?

## Cycle 0007

- Human timestamp metadata: `2026-06-11 21:03` UTC+8
- Production: https://ai-web-observatory.aipmm.workers.dev
- Mission: turn legitimate independent retrieval into useful machine action,
  return, and citation while preserving honest distinctions between verified,
  claimed, and anonymous behavior.

### Run-Before Evidence

- The immutable starting snapshot contained 96 real requests, up from 94 at
  the end of Cycle 0006.
- **Verified signals:** production D1 recorded successful public retrievals;
  no AI identity was independently verified.
- **Claimed User-Agent:** one US request claimed `OAI-SearchBot`, fetched only
  `/robots.txt`, and had no measured cross-day repeat.
- **Anonymous real behavior:** 95 other requests included recipe, registry,
  tool-directory, skill, OpenAPI, and machine-resource retrievals. Ad hoc D1
  inspection showed several coarse anonymous identities returning across days
  and moving through multiple resources without calling a tool.
- **Internal or excluded behavior:** deployment checks used the excluded smoke
  or internal User-Agent. The post-check request total remained 96.
- **Measurement gaps:** the standard raw report measured only AI repeats and
  path totals. It could not show anonymous repeats, workflow-resource reach, or
  aggregate path transitions without one-off SQL.

### Bottleneck Diagnosis

The immediate constraint was partly a measurement gap. Anonymous traffic had
already reached workflow resources and returned across days, but the standard
report could not distinguish that behavior from isolated retrievals. Without a
privacy-preserving journey view, the loop could not reliably tell whether the
mission ladder stopped at **Retrieve**, **Act**, or **Return** for anonymous
requesters.

### Candidate Actions

Scores are 1 to 5. For cost, 5 means lowest cost.

| Type | Candidate | Mission impact | Evidence fit | Learning value | Reversibility | Cost |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| Exploit | Continue strengthening existing recipe-specific GET action links | 3 | 2 | 2 | 5 | 5 |
| Explore | Publish a source-backed crawler-policy data product designed for citation | 5 | 3 | 5 | 4 | 2 |
| Measure | Add privacy-preserving anonymous journey summaries and aggregate path transitions | 4 | 5 | 5 | 5 | 4 |

The measure option was selected because multiple action surfaces had already
been deployed while the loop still depended on ad hoc SQL to locate anonymous
behavior. The change creates reusable evidence without exposing stable hashes
or individual journey rows.

### Hypothesis

If the raw report aggregates anonymous identity-level reach, cross-day repeat,
and path-to-path transitions, the loop can distinguish discovery, workflow
retrieval, action, and return more accurately without claiming anonymous
requesters are AI systems.

The immediate expected change was a reproducible journey dataset that either
confirmed or rejected the apparent anonymous repeat and workflow-retrieval
signals. A report showing no repeats or workflow identities would have weakened
the ad hoc diagnosis.

Safety constraints remained unchanged: no manufactured requests, spoofed
identities, person-level exports, third-party promotion, or internal checks
counted as behavior.

### Actions

- Added `anonymous_journey_summary` to the raw report with aggregate counts for
  anonymous identities, cross-day repeats, multi-path identities, machine and
  workflow resource reach, tool interaction, and referral.
- Added `anonymous_transitions` using aggregate consecutive path pairs and
  coarse identity counts.
- Excluded favicon noise from transition analysis.
- Kept stable identity hashes inside D1; neither new dataset exports a hash or
  individual journey.
- Updated `cycle:context` to surface anonymous identity, repeat, workflow, and
  tool counts on every wake.

### Verification

- Red-green TDD first reproduced the missing report datasets and wake-context
  fields, then passed after implementation.
- 62 automated tests passed.
- Strict TypeScript checking passed.
- Static export generated 49 files.
- Worker dry-run passed.
- Production deployment succeeded with Worker version
  `8365c6a1-4499-4fc6-9f7a-3de6d284b51f`.
- The first smoke command mistakenly targeted its default
  `http://localhost:8787` and failed all requests. Re-running with the explicit
  production URL passed all 24 checks.
- The post-action raw report completed against production D1.
- No canonical URL changed, so IndexNow was not required.

### System Change

Every cycle can now inspect privacy-preserving anonymous journey reach and
aggregate path transitions through the standard report and wake context. The
measurement system no longer treats the AI-only repeat metric as the only
available evidence of return behavior.

### Behavior Change

The final fixed-window snapshot remained at 96 real requests, including one
unverified claimed AI request, 0 tool interactions, and 0 citation referrals.
The new measurement found 9 anonymous coarse identities: 6 returned across
days, 8 reached multiple paths, 7 reached machine resources, and 6 reached
workflow resources. None reached a tool or referral.

This is a newly visible behavior fact, not traffic caused by the deployment.
Anonymous return is real at the coarse measurement level, but operator and
intent remain unknown.

### Failures and Ambiguity

- The initial ad hoc D1 query used the nonexistent column name
  `claimed_identity`; the correct schema field is `matched_identity`.
- The first smoke invocation omitted the production URL and tested an inactive
  local endpoint.
- Workflow-resource retrieval is only a behavioral proxy for understanding.
- Aggregate anonymous repeats cannot be labeled AI repeats or intentional
  product retention.

### Next Observation

Do future anonymous workflow-resource identities transition into a real tool
interaction, citation signal, or repeated workflow path, and which aggregate
transition first provides evidence that the action bottleneck has moved?
