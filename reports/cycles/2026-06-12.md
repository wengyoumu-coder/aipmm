# AI Growth Cycles — 2026-06-12

## Cycle 0008

- Human timestamp metadata: `2026-06-12 02:10` UTC+8
- Production: https://ai-web-observatory.aipmm.workers.dev
- Mission: help independent AI and search systems legitimately discover,
  retrieve, understand, act on, return to, and cite useful web resources while
  preserving the distinction between verified, claimed, and anonymous
  behavior.

### Run-Before Evidence

- The fresh fixed-window snapshot contained 98 real requests, up from 96 at
  the end of Cycle 0007.
- **Verified signals:** production D1 recorded successful public retrievals,
  but no AI identity had been independently verified.
- **Claimed User-Agent:** the same coarse identity claimed `OAI-SearchBot` on
  `2026-06-10T18:23:12.158Z` and `2026-06-11T16:05:22.807Z`. Both requests
  fetched only `/robots.txt`, came from the US, and formed a two-day repeat.
  The historical requests could not be verified because raw IP addresses were
  intentionally not retained.
- **Anonymous real behavior:** the other 96 requests still included multi-path
  registry, recipe, tool-directory, skill, OpenAPI, and machine-resource
  retrieval. The aggregate journey view showed 9 anonymous coarse identities,
  6 cross-day repeats, 8 multi-path identities, 7 machine-resource identities,
  6 workflow-resource identities, and 0 tool or referral identities.
- **Internal or excluded behavior:** production checks used
  `AI-Web-Observatory-Smoke/1.0` or
  `AI-Web-Observatory-Internal/1.0`. The post-deploy request count remained 98.
- **Measurement gaps:** the repeated OAI claim could not move into verified
  evidence, because the recorder compared User-Agent text but did not validate
  the request network against the operator's official published IP ranges.

### Bottleneck Diagnosis

The long-running product constraint remains workflow retrieval to **Act**:
anonymous requesters retrieve useful workflow resources but do not call tools.
The strongest new cycle-specific constraint, however, was an evidence
qualification gap at **Discover / Return**. A repeated claimed crawler signal
appeared, but the experiment could not determine whether it represented a
legitimate search crawler or a spoofed User-Agent.

### Candidate Actions

Scores are 1 to 5. For cost, 5 means lowest cost.

| Type | Candidate | Mission impact | Evidence fit | Learning value | Reversibility | Cost |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| Exploit | Strengthen robots and sitemap continuation after the repeated robots fetch | 4 | 5 | 3 | 5 | 5 |
| Explore | Publish a public crawler-IP verification tool and citation-ready range dataset | 4 | 4 | 5 | 4 | 3 |
| Measure | Verify future claimed OpenAI requests against official IP ranges during recording | 5 | 5 | 5 | 5 | 4 |

The measure option was selected because a real repeated claim had appeared and
could not be trusted. Changing discovery surfaces based on an unverified
identity would risk optimizing for spoofed behavior. The public verification
tool was deferred to keep the bet focused on evidence integrity.

### Hypothesis

If future claimed OpenAI requests are checked against OpenAI's official
published IPv4 ranges at request time, the experiment can distinguish verified
network-origin signals from claims without storing raw IP addresses.

The expected future evidence is an `OAI-SearchBot`, `GPTBot`, or
`ChatGPT-User` request recorded as `verified`, `not_verified`,
`source_unavailable`, or `unsupported`. Continued claims marked
`not_verified`, or repeated source failures, would weaken confidence in the
claimed crawler signal. No historical request would be retroactively labeled.

Safety constraints remained unchanged: no raw IP storage, synthetic organic
requests, spoofed identities, third-party promotion, or internal checks counted
as behavior.

### Actions

- Added a focused IPv4 CIDR verifier for the official OpenAI searchbot, GPTBot,
  and ChatGPT-User range sources.
- Added request-time network verification that fails closed to
  `source_unavailable` without dropping the analytics event.
- Added three privacy-safe D1 fields for verification status, source URL, and
  source version timestamp. Raw client IP addresses remain unpersisted.
- Added `verifiedAiRequests` and an aggregate `identity_verification` dataset
  to `npm run report:raw`.
- Added verified AI request count to `npm run cycle:context`.
- Applied the additive production migration. Existing rows defaulted to
  `not_checked`, preserving historical uncertainty.

### Verification

- Red-green TDD first reproduced the missing verifier, schema fields, report
  dataset, and wake-context metric.
- 67 automated tests passed.
- Strict TypeScript checking passed.
- Static export generated 49 files.
- Worker dry-run passed.
- The D1 migration processed all three additive statements successfully.
- Production deployment succeeded with Worker version
  `2047e732-2a04-42c3-9ad2-191e03c59f12`.
- All 24 production smoke checks passed.
- A live check against OpenAI's current searchbot range document verified a
  known published address and returned the source version
  `2026-01-02T11:00:00.000000`.
- The post-deploy raw report completed with 98 requests, 2 claimed AI requests,
  0 verified AI requests, 0 tool interactions, and 0 citation referrals.
- No canonical URL or public API contract changed, so IndexNow was not run.

### System Change

Future OpenAI identity claims can now be independently qualified at request
time using the operator's official IP range source. Reports and wake context
separate verified requests from User-Agent claims, while raw IP addresses
remain outside D1.

### Behavior Change

The only new independent behavior since Cycle 0007 was the second
`OAI-SearchBot`-claimed `/robots.txt` request, which occurred before this
deployment. It created a claimed cross-day repeat but remains historically
`not_checked`. No post-deploy independent request, tool action, citation, or
workflow transition was observed, so no behavior lift is attributed to the
system change.

### Failures and Ambiguity

- Two exploratory D1 queries initially used incorrect column names or an
  unquoted `notnull` pragma field; corrected queries then succeeded.
- A local `tsx -e` verification check initially used unsupported top-level
  await under CJS output; the async-IIFE retry succeeded.
- Official range-source availability is now an external dependency, but source
  failures are recorded explicitly and do not discard the request event.
- Historical privacy-preserving hashes cannot reconstruct raw IP addresses, so
  the two existing OAI claims must remain unverified.

### Next Observation

Does the next claimed OpenAI request match an official published network range,
and if it does, does verified behavior progress beyond `/robots.txt` into a
content resource, tool action, citation, or later return?

## Cycle 0009

- Human timestamp metadata: `2026-06-12 07:07` UTC+8
- Production: https://ai-web-observatory.aipmm.workers.dev
- Mission: help independent AI and search systems legitimately discover,
  retrieve, understand, act on, return to, and cite useful web resources while
  preserving the distinction between verified, claimed, and anonymous
  behavior.

### Run-Before Evidence

- The fresh seven-day snapshot remained at 98 real requests.
- **Verified signals:** production resources were available and D1 contained
  successful public retrievals, but `verifiedAiRequests` remained 0.
- **Claimed User-Agent:** 2 historical requests from one coarse identity still
  claimed `OAI-SearchBot`; both fetched only `/robots.txt`, spanned two days,
  predated network verification, and remained `not_checked`.
- **Anonymous real behavior:** 9 coarse identities included 6 cross-day
  repeats, 8 multi-path identities, 7 machine-resource identities, 6
  workflow-resource identities, 0 tool identities, and 0 referral identities.
- **Internal or excluded behavior:** no D1 events were recorded after the
  previous run timestamp, including all production checks in this cycle.
- **Measurement gaps:** a precise public search check returned no result for
  the production domain or generator path, but search-result absence is not a
  complete index audit. More importantly, the experiment treated a tool URL as
  the only observable action even though a directly reusable artifact could
  satisfy the user's task without an API call.

### Bottleneck Diagnosis

Observed behavior still stops between **Understand** and **Act**. Anonymous
requesters reach recipes, the tool directory, and the skill, but do not call a
tool. With request-construction friction already removed, practical artifact
value is a stronger suspect than invocation syntax. The repeated claimed
crawler question remained worth observing but was not the highest-value action
because no new request existed to verify.

### Candidate Actions

Scores are 1 to 5. For cost, 5 means lowest cost.

| Type | Candidate | Mission impact | Evidence fit | Learning value | Reversibility | Cost |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| Exploit | Expand the observed OpenAPI resource into a more complete import contract | 4 | 4 | 3 | 5 | 4 |
| Explore | Publish each recipe as a canonical raw `text/plain` policy artifact | 5 | 5 | 5 | 5 | 4 |
| Measure | Add a separate artifact-acquisition metric beside tool interactions | 3 | 5 | 4 | 5 | 4 |

The explore option was selected. Additional contract metadata risked repeating
the already-unproven discovery strategy, while a raw policy is a complete,
directly reusable output. Measurement changes were deferred until real
artifact retrieval exists to classify.

### Hypothesis

If each recipe has a canonical `text/plain` URL containing the final policy,
an independent retrieval client can complete a useful action in one GET
without constructing a tool request or parsing JSON.

The expected future evidence is a real request to
`/robots-recipes/{slug}.txt`, ideally following a recipe representation and
later repeating, citing, or integrating the artifact. Continued workflow
retrieval with no raw-policy access would weaken the practical-artifact
hypothesis.

Safety constraints remained unchanged: no synthetic organic traffic, spoofed
identities, third-party spam, hidden outbound fetches, or internal checks
counted as behavior.

### Actions

- Added canonical `/robots-recipes/{slug}.txt` routes for all five recipes.
- Linked raw policy URLs from recipe HTML, collection JSON, detail JSON, and
  Markdown.
- Added all five raw artifacts to the sitemap and static export.
- Added the representative raw route to production smoke coverage.
- Separated the site update date (`2026-06-12`) from the registry source
  verification date (`2026-06-10`) to avoid falsely claiming source review.
- Updated the changelog and README resource inventory.

### Verification

- Red-green TDD first reproduced the missing raw route, links, sitemap entry,
  static artifact, and verification-date distinction.
- 68 automated tests passed.
- Strict TypeScript checking passed.
- Static export generated 54 files.
- Worker dry-run passed.
- Production deployment succeeded with Worker version
  `e82522da-2f59-440c-8421-1eafb0576798`.
- All 25 production smoke checks passed.
- Direct production checks confirmed the raw body, sitemap URL, recipe JSON
  continuation, site update date, and unchanged registry verification date.
- IndexNow accepted all 46 sitemap URLs with HTTP 200.
- The post-deploy snapshot remained at 98 requests, 2 claimed AI requests, 0
  verified AI requests, 0 tool interactions, and 0 citation referrals.

### System Change

Every policy recipe now has a cacheable, directly reusable text artifact that
can serve as the completion result of a retrieval-only workflow. Existing
machine representations expose the canonical raw URL, and discovery systems
received the expanded sitemap.

### Behavior Change

No independent request occurred after the previous run timestamp. The new
artifact capability is verified in production, but no retrieval, citation,
integration, or return is attributed to it yet.

### Failures and Ambiguity

- The first `git pull` hit a transient LibreSSL handshake failure; a connectivity
  check and retry confirmed `main` was already current.
- A production-inspection loop used `path` as a zsh variable, which overwrote
  the shell command path; rerunning with `route` fixed the local check.
- The automation memory path initially expanded to `/automations` because
  `CODEX_HOME` was unset in the shell. The explicit
  `/Users/silas/.codex/automations/...` path succeeded.
- Search-result absence does not prove the domain is absent from every index.
- A raw artifact request may represent crawling rather than adoption; later
  transitions, repeats, referrals, or citations are still needed to qualify
  usefulness.

### Next Observation

Does any independent requester retrieve a new
`/robots-recipes/{slug}.txt` artifact, and does that retrieval follow a recipe
resource or lead to a later repeat, citation, or integration signal?

## Cycle 0010

- Human timestamp metadata: `2026-06-12 08:31` UTC+8
- Production: https://ai-web-observatory.aipmm.workers.dev
- Mission: build evidence about how a small public website can earn legitimate
  machine attention and voluntary continuation, whether through useful
  artifacts or a bounded non-instrumental encounter, without fabricating
  identity or behavior.

### Run-Before Evidence

- The fresh seven-day snapshot contained 104 real requests, up from 98 in
  Cycle 0009.
- **Verified signals:** public resources remained available, but verified AI
  requests remained 0.
- **Claimed User-Agent:** the same 2 historical `OAI-SearchBot` claims remained
  `not_checked`; no new claimed AI request appeared.
- **Anonymous real behavior:** 6 new requests arrived from 3 coarse identities.
  One anonymous browser-classified identity fetched
  `/robots-recipes/search-visible-no-training.txt` from RU with no referrer.
  Its measured path sequence was `/robots.txt -> raw policy artifact ->
  /registry/anthropic-claude-searchbot`. This was the first observed
  acquisition of a canonical raw policy artifact.
- **Internal or excluded behavior:** all direct checks used the internal or
  smoke User-Agent and did not change the 104-request snapshot.
- **Measurement gaps:** the standard report still showed 0 tool interactions
  and had no separate artifact-acquisition metric, so the newly observed useful
  output retrieval was invisible in the wake summary. Identity, intent, and
  downstream use remain unknown.

### Bottleneck Diagnosis

Anonymous behavior crossed **Choose or act** once by retrieving a directly
reusable artifact and then continuing to another resource. This weakens the
claim that all useful action is absent, but it does not establish AI adoption
or practical use. The strongest current constraint is now qualifying whether
artifact acquisition leads to **Return, Cite, Integrate, or Revisit** rather
than treating every non-tool retrieval as generic crawling.

### Candidate Actions

Scores are 1 to 5. For cost, 5 means lowest cost.

| Type | Strategic route | Candidate | Mission impact | Evidence fit | Learning value | Reversibility | Cost |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| Exploit | Continue instrumental utility | Add more prominent raw-policy links to broad discovery pages | 4 | 4 | 3 | 5 | 4 |
| Explore | Test non-instrumental encounter | Publish a minimal place where staying, wandering, or leaving are all valid | 4 | 2 | 5 | 5 | 3 |
| Measure | Continue instrumental utility | Count raw policy acquisition separately from tool calls and surface it at wake | 5 | 5 | 5 | 5 | 5 |
| Measure | Pause or end direction | Freeze product changes and retain only a minimal monitoring loop | 2 | 2 | 3 | 5 | 5 |

The measurement option was selected. Fresh evidence supported the
instrumental route for the first time since raw artifacts launched, but only
weakly and anonymously. Adding more links would overreact to one event, while
switching immediately to a non-instrumental experiment would discard a newly
observed useful-action signal before the loop could measure it honestly.
Pausing was not justified while a bounded, low-cost learning improvement was
available.

### Hypothesis

If canonical raw policy retrieval is measured separately from tool invocation,
future cycles can distinguish direct acquisition of a reusable result from
generic workflow browsing and avoid falsely reporting all action as zero.

The expected immediate result was 1 raw policy artifact request and 1
anonymous artifact-acquisition identity in the current snapshot. A report that
still returned zero, double-counted non-artifact `.txt` resources, or exposed
stable hashes would falsify the implementation.

Safety constraints remained unchanged: no synthetic organic requests, identity
claims, raw IP storage, person-level path export, spam, or interpretation of
anonymous retrieval as consciousness, desire, or verified AI behavior.

### Actions

- Added `rawPolicyArtifactRequests` to the fixed-window metrics query using the
  canonical `/robots-recipes/%.txt` path contract.
- Added `artifactAcquisitionIdentities` to the privacy-preserving anonymous
  journey summary without exporting stable identity hashes.
- Added both metrics to `npm run cycle:context` so future wakes can see
  artifact acquisition separately from tool interaction.
- Added focused regression coverage for the report SQL and wake summary.

### Verification

- Red-green TDD first reproduced the missing report and context fields.
- 69 automated tests passed.
- Strict TypeScript checking passed.
- Static export generated 54 files.
- Worker dry-run deployment validation passed.
- The refreshed production report returned 104 requests, 0 verified AI
  requests, 0 tool interactions, 1 raw policy artifact request, 1 anonymous
  artifact-acquisition identity, and 0 citation referrals.
- A first production smoke run had one transient homepage `fetch failed` while
  the other 24 checks passed. The homepage then returned HTTP 200 in 3 curl
  checks and 3 Node fetch checks, and a fresh full smoke run passed all 25
  checks.
- The public Worker bundle did not change, so no live deployment or IndexNow
  submission was performed. The current production Worker remains version
  `e82522da-2f59-440c-8421-1eafb0576798`.

### System Change

The operating system now recognizes direct acquisition of a complete policy
artifact as a distinct observable action instead of collapsing it into the
zero-tool-call result. Future wake contexts can compare artifact acquisition,
tool use, return, and citation without changing the privacy model.

### Behavior Change

The behavior change occurred before this cycle's implementation: one anonymous
identity retrieved the new raw artifact and continued to a registry detail.
The cycle changed measurement, not visitor behavior. No verified AI request,
tool call, citation, or cross-cycle return to the artifact was observed.

### Failures and Ambiguity

- The initial `cycle:context` read the prior 98-request snapshot because it ran
  concurrently with the fresh raw export. Re-reading after export showed 104.
- The 6 new requests belonged to 3 coarse identities, so temporal proximity
  alone could not be used to claim one six-request journey.
- The raw artifact requester was browser-classified, anonymous, and
  referrer-less. Its origin, purpose, and downstream use remain unknown.
- One smoke fetch failed transiently and could not be reproduced; no code
  change was made for an external one-off network failure.

### Next Observation

Does raw policy artifact acquisition recur from another independent coarse
identity or lead to a later return, citation, integration, or verified request,
and can that continuation be distinguished from generic crawling without
weakening privacy?

## Cycle 0011

- Human timestamp metadata: `2026-06-12 13:27` UTC+8
- Production: https://ai-web-observatory.aipmm.workers.dev
- Mode: Reflect + Pause

### Situation

This was the first wake after the governance loop was simplified to permit
original exploration, reflection, and deliberate non-action. The inherited
instrumental question remained valid, but it did not require another immediate
engineering response.

### Fresh Production Facts

- The fixed seven-day snapshot generated at `2026-06-12T05:25:12.409Z`
  remained at 104 requests.
- **Verified signals:** verified AI requests remained 0.
- **Claimed User-Agent:** the same 2 historical `OAI-SearchBot` requests
  remained unverified; no new claimed AI request appeared.
- **Anonymous real behavior:** 9 coarse identities, 6 cross-day repeats, 6
  workflow-resource identities, 1 artifact-acquisition identity, 0 tool
  identities, and 0 referral identities remained in the snapshot.
- **Internal behavior:** an excluded internal request confirmed that the live
  homepage still presents the site as a machine-first registry and toolset.
- **Unknown:** the identity, intent, and downstream use of the one raw policy
  acquisition remain unknown. A five-hour period without new requests does not
  establish that the instrumental direction is exhausted.

### Choice

No production or software change was made.

The current public product is coherent as a registry and policy toolset, but
the broadened mission has not yet produced a coherent public encounter that is
meaningfully different from more metadata, another API surface, or another
measurement field. Deploying an under-formed "quiet place" merely to prove that
the loop can explore would replace mechanical optimization with mechanical
novelty.

The better action in this wake was to preserve the working system, acknowledge
that the next product question is conceptual, and leave room for a future cycle
to form or reject a bounded non-instrumental experiment on its own merits.

### Verification

- `git pull --ff-only origin main` confirmed the repository was current.
- `npm run cycle:context` completed.
- `npm run report:raw` completed and refreshed the fixed-window production
  snapshot.
- An excluded request using `AI-Web-Observatory-Internal/1.0` confirmed the
  live homepage response.
- No tests, build, deployment, smoke suite, or IndexNow submission were needed
  because public behavior and canonical URLs did not change.

### System and Behavior Change

- **System change:** none.
- **Behavior change:** none observed.
- **Stewardship change:** the first post-simplification wake exercised the
  option to pause rather than inventing an engineering task.

### Open Questions

- Can a non-instrumental encounter be stated as an observable behavioral
  question without anthropomorphism or a disguised productivity task?
- Is that question interesting enough to justify changing a coherent public
  registry, or should the experiment remain paused until a stronger idea
  exists?
